# Override with env: SKILLS_DIR, SKILLS_TARGETS (colon-separated paths)

skills_dir = "~/.agents/skills"

[targets]
# codex = "~/.codex/skills" # don't sync; contains skills installed by the application
claude = "~/.claude/skills"
kiro = "~/.kiro/skills"
